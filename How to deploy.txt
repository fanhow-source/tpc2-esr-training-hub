之後你的完整流程就是：

cd C:\Users\CharlieTseng\Desktop\tpc2-esr-training-hub
python -m mkdocs serve

本地確認 OK 後 Ctrl + C

再用powershell跑：

.\deploy.ps1
python -m mkdocs gh-deploy --force --clean

這樣就會自動 build、commit、push、deploy，最後開啟 GitHub Pages。


如果出現一些紫色文字，表示 push to GitHub 沒成功
再執行

有時新版本跑不出來，也可以用
python -m mkdocs build --clean